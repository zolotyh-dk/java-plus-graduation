spring:
  main.banner-mode: OFF
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:ewm_stats
    username: stats
    password: stats
  jpa:
    hibernate:
      ddl-auto: validate